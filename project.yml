name: PhotoAlbumPlayer
options:
  bundleIdPrefix: com.yourcompany
  deploymentTarget:
    iOS: 16.0
  xcodeVersion: "15.0"

targets:
  PhotoAlbumPlayer:
    type: application
    platform: iOS
    sources:
      - Sources
    settings:
      base:
        INFOPLIST_FILE: Info.plist
        PRODUCT_BUNDLE_IDENTIFIER: com.yourcompany.photoalbumplayer
        CODE_SIGN_STYLE: Manual
        DEVELOPMENT_TEAM: ""
        CODE_SIGN_IDENTITY: ""
    dependencies: []
